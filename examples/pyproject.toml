[project]
name = "uv2nix"
version = "0.1.0"
description = "Example project"
requires-python = ">=3.9"
dependencies = [
    "typer",
    "numpy"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
